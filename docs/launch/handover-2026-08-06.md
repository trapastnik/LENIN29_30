# Записка оркестратору · пауза 2026-08-06

Предыдущий оркестратор ушёл на `/clear`. Это точка возобновления. Читается
вместе с `CLAUDE.md` (§1–13 — все правила и решения) и `memory/MEMORY.md`.

## Как поднять контекст

1. `CLAUDE.md` — контракт, зоны, правила. Всё сведённое — там.
2. Зоны **не очищались** — у них полный контекст. `list_sessions` по `cwd`
   даёт их id (таблица §3 могла устареть, сверяй), `send_message` — канал.
3. Состояние веток: `git fetch origin && git log --oneline origin/main..origin/<ветка>`.

## Работа ОСТАНОВЛЕНА dvn (пауза)

Все зоны стоят чисто, WIP на ветках, в `main` не сведён. Причину паузы
не знаю — жди слова dvn, прежде чем поднимать.

| Зона | Ветка head | Готово | Точка возобновления |
|---|---|---|---|
| `maps` | `793a629` | единая база (берег/реки/соседи в манере, Альберс −0.47%), записана в `docs/maps-pipeline.md` | **шаг 1: РСФСР-полигон на базу** (её вариант, согласован) |
| `design` | `b3e2753` | rgb-канальные токены (снят дрейф цвета) | холодный хром / снятие сепии — **в каталог `brand.html` сначала, показ dvn до разделов** |
| `ui` | `de4145e` | каркас рельса связанных на `parties.html` (работает, первый потребитель глубоких ссылок) | ревью эталона → тираж в `state-card` + `people-detail` |
| `content` | `2bcf372` | связи (`_backlinks.json` out/refs/mutual), письмо, орфография — всё сведено | музей |
| `simbirsk` | `1ef068d` | всё сведено | музей |

`main` = `e6285ab`. `design` держит 8 карточек каталога (`2da21aa`) — не публикует.

## Ключевые решения дня (ещё НЕ в §CLAUDE.md — внеси при возобновлении)

1. **Единая мягкая база вместо ожидания карты от музея.** dvn: «делаем,
   точно будем». База НЕ подробная (мягкая, акварельная манера референса).
   Строим сами из открытых геоданных (Natural Earth, public domain, dvn
   одобрил). Снимает блокер 7 регионных территорий (держим обе стороны →
   рассогласованию неоткуда взяться). Разбор — `docs/maps-pipeline.md`
   «Единая мягкая база». `map-camera.js`/хаб-полёт — ПОСЛЕ базы.
2. **Хаб-полёт — шестой режим** (добавляем к плиткам, не заменяем). Летаем
   по территориям И событиям (у событий гео нет — для демо ставим руками).
   Демо на растровой… нет, на новой базе; нырок в демо = кроссфейд (не
   геометрический перелёт). Решено делать после базы.
3. **Сближение стиля с проектом `BMK/38-42`** (тот же музей, те же
   шрифты/RAL-палитра). Их канон: серый фон, красный точечно НЕ заливкой,
   полосы к кромке. Три расхождения хрома: активное уже латунь (сделано);
   плитки тёплые→холодные (= снятие сепии, путь «2 для хрома + 3 для
   факсимиле»); полосы к кромке. Взять их приёмы: rgb-токены (сделано),
   раздельный `--ui-scale`/`--content-scale` (прототип). Канал с их
   координатором (`LENIN 3842 - COORD`) открыт, обмен ждёт слова dvn.
4. **Рельс связанных справок** — ПРАВЫЙ рельс (экран горизонтальный),
   не нижний блок. Три группы: `out`/`refs`/`mutual`. Каркас `ui` готов
   на `parties`, вид точки чипа ждёт примитив `#c-related` от `design`.
5. **Письмо музею** (`docs/zapros-muzey.md`) — 18 пунктов, готово к отправке
   dvn. Пункт 6 переписан: 6а (подробную базу) убран (строим сами),
   остался только запрос границ губерний/фронтов 1918. Пять пунктов
   останавливают работу. Приложение `zapros-muzey-annex-media.md`.
6. **Контраст**: измеритель `ui` переписан на пиксельный (`contrast:check`),
   119 ложных → 16 реальных. Правило палитры: цвет как мелкий текст не
   читается, носитель цвета → форма. Остаток (Венн, золото, «Красные·21»)
   за палитрой `design`.

## Ждёт РЕШЕНИЯ dvn

- 🔴 **брендбук vs 38-42**: красный параллелограмм-мотив — брендбук ЗА,
  канон соседа против. Чей приоритет. `design` молча не решает;
- курс на холодный хром — «совсем» или «чуть»;
- слово по обмену с 38-42 (оба координатора ждут);
- отправка письма музею;
- чат `kiosk` (аттрактор, сброс сеанса, сброс языка — не начато);
- сброс языка на русский при простое (90 сек — предложено);
- новая карта-референс для базы (dvn обещал дать).

## На возобновлении, порядок

1. Дождаться слова dvn (пауза).
2. Свести три ветки (`maps`/`design`/`ui`) в `main`, `npm run check`, деплой.
3. Поднять зоны с точек возобновления выше.
4. Внести решения дня 1–6 в `CLAUDE.md` §, чтобы следующий `/clear`
   их уже нашёл в контракте, а не в записке.
